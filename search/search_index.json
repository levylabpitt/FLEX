{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"FLEX","text":"<p>Framework for Laboratory EXperiments \u2014 a modular Python platform for running laboratory experiments: instrument control, autonomous sweeps, data files, experiment records, and lab-specific integrations, with one-command setup for new users.</p> <pre><code>irm flex.levylab.org/install.ps1 | iex\n</code></pre> <pre><code>from flex import Experiment, Scan, sweep, SimulatedInstrument\nimport numpy as np\n\nwith Experiment(\"jane\") as exp:\n    sim = exp.add(SimulatedInstrument, \"demo\")\n    gate = sim.add_sim_parameter(\"gate\", unit=\"V\")\n    x = sim.add_sim_parameter(\"x\", initial=0.5, unit=\"V\")\n\n    Scan(sweep(gate, np.linspace(0, 1, 101), delay=0.01)) \\\n        .measure(sim.parameters[\"x\"]) \\\n        .on_abort(lambda: gate(0)) \\\n        .run(exp, name=\"gate sweep\")\n</code></pre> <p>That's a complete, autonomous, safely-abortable experiment: data lands in an HDF5 file, metadata in SQLite, and a per-experiment log file \u2014 all under your user data directory, no configuration needed. Swap <code>SimulatedInstrument</code> for a real one to run it on hardware.</p>"},{"location":"#where-to-go","title":"Where to go","text":"<ul> <li>Quickstart \u2014 install and run your first experiment in   five minutes.</li> <li>Concepts \u2014 how the pieces fit together:   packages, instruments, experiments, ecosystems, the dashboard.</li> <li>Tutorials \u2014 write a driver, build your   lab's ecosystem, set up a LevyLab station.</li> <li>Reference \u2014 every driver and integration,   generated from the code.</li> <li>Migrating from v1 \u2014 v1 stays on the <code>main</code>   branch; this maps every v1 concept to its v2 home.</li> </ul>"},{"location":"#the-short-version","title":"The short version","text":"You want to\u2026 FLEX gives you talk to an instrument <code>VISAInstrument</code> / <code>ZMQInstrument</code> / <code>TCPInstrument</code> / <code>SerialInstrument</code> base classes, and a catalog of ready drivers run a sweep safely <code>Scan</code> + <code>sweep</code>: Ctrl-C finalizes the file, marks the measurement aborted, runs your cleanup keep records every experiment and measurement is recorded (SQLite by default, PostgreSQL for labs) share a lab setup an ecosystem: one TOML manifest, one <code>flex ecosystem use</code> command manage it all visually <code>python -m flex dashboard</code> \u2014 packages, drivers, ecosystems, and experiment browsing in the browser"},{"location":"build-an-ecosystem/","title":"Build an Ecosystem","text":"<p>An ecosystem is your lab's FLEX setup in one TOML manifest: the packages to install plus every setting. Share the file; new members run one command.</p> <pre><code>flex ecosystem use mylab.toml\n</code></pre>"},{"location":"build-an-ecosystem/#the-manifest","title":"The manifest","text":"<pre><code>[ecosystem]\nname = \"mylab\"\npackages = [\"flex-datatypes\", \"flex-drivers\"]   # installed on activation\n\n[lab]\nname = \"mylab\"\nstation = \"cryo1\"\n\n[db]\nbackend = \"sqlite\"            # or \"postgres\" with dsn = \"postgresql://...\"\n\n[storage]\nbackend = \"local\"             # or \"nextcloud\" with url/user (+ NEXTCLOUD_PASSWORD env)\n\n[data]\nwriter = \"hdf5\"               # or \"tdms\"\nroot = \"D:/data\"\n\n[comms]\nbackend = \"none\"              # or \"asana\" (flex-asana) -- see its package docstring for config\n\n[hooks]                       # dotted refs, subscribed to experiment events\non_experiment_end = [\"some_package.hooks:notify\"]\n\n[drivers]\nenabled = [\"srs.sr7270\"]\n\n[stations.cryo1.instruments.lockin]\ndriver = \"srs.sr7270\"\naddress = \"USB0::0x0A2D::0x001B::12345::RAW\"\n</code></pre> <p>Every section is optional \u2014 omit it and the default applies. Backends may define their own extra keys (like <code>dsn</code> above); they pass straight through.</p>"},{"location":"build-an-ecosystem/#how-the-configuration-is-found","title":"How the configuration is found","text":"<ol> <li>explicit path (<code>Experiment(config=...)</code> / <code>load_config(path)</code>)</li> <li>the <code>FLEX_CONFIG</code> environment variable</li> <li><code>./flex.toml</code> in the working directory</li> <li>the activated config (<code>%LOCALAPPDATA%/flex/config.toml</code>)</li> <li>nothing \u2192 pure defaults (SQLite + HDF5 + local files)</li> </ol> <p><code>flex ecosystem show</code> prints the resolved result; <code>flex ecosystem validate mylab.toml</code> checks a manifest and whether its components are installed.</p>"},{"location":"build-an-ecosystem/#stations","title":"Stations","text":"<p><code>[stations.*]</code> describes which instruments live where. Then:</p> <pre><code>with Experiment(\"jane\") as exp:\n    exp.load_station(\"cryo1\")      # constructs and registers every instrument\n    exp.lockin.x()                 # ready to use\n</code></pre> <p><code>flex instruments --probe</code> test-connects everything from the shell.</p>"},{"location":"build-an-ecosystem/#extending-flex-itself","title":"Extending FLEX itself","text":"<p>Labs add their own components as normal Python packages:</p> <ul> <li>a driver package: <code>flex new package flex-drivers-mylab</code> scaffolds one \u2014   fill drivers in, list them in its <code>CATALOG</code>, and they appear in   <code>flex list --drivers</code>;</li> <li>a DB backend / data writer / storage backend / comms backend: subclass   <code>MetadataStore</code>, <code>DataWriter</code>, <code>StorageBackend</code>, or <code>CommsBackend</code> from   <code>flex-core</code> and export a <code>{name: \"module:Class\"}</code> registry dict from your   package \u2014 the name you register is what goes in the manifest;</li> <li>hooks: any function <code>fn(event, experiment, **payload)</code>, referenced from   <code>[hooks]</code>.</li> </ul> <p>To make a component discoverable by <code>flex install</code>/<code>flex list</code> without editing an installed package, add it to a <code>catalog.local.json</code> next to your active ecosystem config, e.g. <code>{\"flex-drivers-mylab\": {\"registries\": {\"drivers\": \"flex_drivers_mylab:CATALOG\"}}}</code>.</p> <p>The FLEX repo hosts official packages in <code>packages/</code>; PRs welcome \u2014 the LevyLab drivers (<code>flex_drivers.levylab</code>) alongside <code>flex-datatypes</code>, <code>flex-nextcloud</code>, <code>flex-asana</code> are the reference implementation.</p> <p>To make your own lab's manifest activatable by bare name (<code>flex ecosystem use mylab</code>) rather than a full path, drop it in this repo's own <code>ecosystems/</code> folder (not inside any package \u2014 see <code>flex.pkgmanager.ecosystems</code>). Only <code>default.toml</code> ships inside <code>flex-core</code> itself; everything else here is this repo's own content, so a fork can add, remove, or replace manifests without touching core at all.</p>"},{"location":"hardware-checklist/","title":"On-Hardware Validation Checklist (v2 acceptance)","text":"<p>Run in the lab, on a station with live Instrument-Framework apps. Tick every box before switching production work to v2.</p>"},{"location":"hardware-checklist/#setup","title":"Setup","text":"<ul> <li>[ ] Fresh venv: <code>pip install -e</code> the workspace packages (or <code>uv sync</code>)</li> <li>[ ] <code>flex ecosystem use levylab</code> completes; <code>flex ecosystem show</code> looks right</li> <li>[ ] <code>NEXTCLOUD_PASSWORD</code>, <code>ASANA_ACCESS_TOKEN</code>, <code>ASANA_EXPERIMENTS_PROJECT_GID</code>       set; <code>flex ecosystem validate levylab</code> all green</li> </ul>"},{"location":"hardware-checklist/#instruments","title":"Instruments","text":"<ul> <li>[ ] <code>flex instruments --probe</code> reaches every configured IF app</li> <li>[ ] <code>CESession()</code> boots from the real Configure Experiments VI file;       the HTML summary shows the correct instruments + wiring</li> <li>[ ] A slow/hung IF app produces a <code>TimeoutError</code>, and the next call       succeeds (REQ-socket recovery)</li> <li>[ ] <code>exp.update()</code> picks up an instrument added in the VI while running</li> </ul>"},{"location":"hardware-checklist/#measurement-pipeline","title":"Measurement pipeline","text":"<ul> <li>[ ] A short gate sweep writes a TDMS file readable by the lab DataViewer       (channel layout, units, metadata) \u2014 this is the highest-risk item</li> <li>[ ] The file uploads to Nextcloud on measurement end; pointer in the DB</li> <li>[ ] Experiment + measurement rows appear in PostgreSQL</li> <li>[ ] Ctrl-C mid-sweep: file finalized, <code>aborted</code> flag set, <code>on_abort</code> ramps       the gate down, instruments still responsive afterwards</li> <li>[ ] An Asana task is created on experiment start and its end time fills in       on end (check the configured project directly)</li> </ul>"},{"location":"hardware-checklist/#ux","title":"UX","text":"<ul> <li>[ ] <code>python -m flex dashboard</code>: Drivers tab installs/enables a driver; Experiments       tab shows the runs from today</li> <li>[ ] Jupyter: <code>CESession()</code> renders the summary card; cells are logged as notes</li> </ul>"},{"location":"hardware-checklist/#cleanup-decisions-to-confirm-with-the-lab","title":"Cleanup decisions to confirm with the lab","text":"<ul> <li>[ ] Revoke the Asana token committed in v1 (<code>src/flex/asana/Asana.py</code> on main)</li> <li>[ ] Plan the v1 <code>exp</code>/<code>meas</code>/<code>cell_log</code> PostgreSQL migration</li> <li>[ ] Confirm <code>flex.lv</code> (LabVIEW call helpers) can stay retired</li> <li>[ ] Decide priority for the deferred .NET/COM drivers (Newport, Ophir, Sphere)</li> </ul>"},{"location":"levylab/","title":"LevyLab Guide","text":"<p>The LevyLab ecosystem connects FLEX to the lab's LabVIEW Instrument Framework (IF): each instrument runs as an IF app exposing a JSON-RPC API over ZMQ, experiments are configured in the Configure Experiments VI, data goes to TDMS + Nextcloud, metadata to the lab PostgreSQL, and Asana gets a task per experiment via <code>flex-asana</code> \u2014 the Asana API directly, no n8n.</p>"},{"location":"levylab/#setup-once-per-machine","title":"Setup (once per machine)","text":"<pre><code>irm flex.levylab.org/install.ps1 | iex\nflex ecosystem use levylab\n$env:NEXTCLOUD_PASSWORD = '...'              # or add to the machine environment\n$env:ASANA_ACCESS_TOKEN = '...'\n$env:ASANA_EXPERIMENTS_PROJECT_GID = '...'\n</code></pre>"},{"location":"levylab/#cesession","title":"CESession","text":"<p><code>CESession</code> is an <code>Experiment</code> bootstrapped from the Configure Experiments VI: it reads <code>Control Experiment.json</code>, connects a driver to every configured IF app (matched by LabVIEW class), and attaches them by their CE type.</p> <pre><code>from flex import CESession, Scan, sweep\nimport numpy as np\n\nwith CESession() as exp:                       # user/device/wiring from the VI\n    exp.DAQ.set_ao_dc(1, 0.0)\n    print(exp.wiring)                          # {lockin ch: (electrode, label)}\n\n    gate = exp.DAQ.parameters.get(\"ao1_dc\") or (lambda v: exp.DAQ.set_ao_dc(1, v))\n    Scan(sweep(gate, np.linspace(0, 1, 101), delay=0.1)) \\\n        .measure(results=exp.DAQ.get_results) \\\n        .on_abort(lambda: exp.DAQ.set_ao_dc(1, 0.0)) \\\n        .run(exp, name=\"gate sweep\")\n</code></pre> <p>Everything from the base <code>Experiment</code> applies: measurements write TDMS files (per the levylab config), records land in PostgreSQL, <code>exp.update()</code> picks up instruments added in the VI while running, and an Asana task tracks the run from start to end (see Experiments &amp; data).</p>"},{"location":"levylab/#talking-to-if-apps-directly","title":"Talking to IF apps directly","text":"<p>Every IF driver is a <code>ZMQInstrument</code>; drivers are thin, and raw access is always available:</p> <pre><code>from flex_drivers.levylab.lockin import Lockin\n\nwith Lockin(\"lockin\", \"tcp://localhost:29170\") as li:\n    li.set_ao_amplitude(1, 0.1)         # sends setAO_Amplitude\n    print(li.get_results())\n    print(li.help())                    # the IF app's own method list\n    li.call(\"setSweep\", {...})          # any method, verbatim\n</code></pre> <p>Errors from the IF (<code>error</code> in the JSON-RPC reply) raise <code>ZMQInstrumentError</code>; a dead app raises <code>TimeoutError</code> and the connection recovers on the next call.</p>"},{"location":"levylab/#non-if-instruments","title":"Non-IF instruments","text":"<p>Nothing requires the Instrument Framework: a plain VISA/serial instrument (e.g. <code>flex_drivers.srs.SR7270</code>) registers into the same session next to IF drivers:</p> <pre><code>with CESession() as exp:\n    exp.add(SR7270, \"lockin2\", serial=\"12345\")\n</code></pre>"},{"location":"levylab/#testing-drivers-without-labview","title":"Testing drivers without LabVIEW","text":"<p><code>flex_protocols.testing.FakeIFServer</code> is an in-process fake IF app:</p> <pre><code>from flex_protocols.testing import FakeIFServer\n\nwith FakeIFServer({\"getResults\": {\"X\": [1.0]}}) as server:\n    li = Lockin(\"li\", server.address)\n    assert li.get_results() == {\"X\": [1.0]}\n</code></pre>"},{"location":"levylab/#the-historical-database","title":"The historical database","text":"<p>v2 writes to its own PostgreSQL tables (<code>experiments</code>, <code>measurements</code>, <code>notes</code>). The v1 tables (<code>exp</code>, <code>meas</code>, <code>cell_log</code>) are untouched; migrating them is a planned one-off \u2014 see the migration notes.</p>"},{"location":"migration-v1-to-v2/","title":"Migrating from FLEX v1 to v2","text":"<p>FLEX v2 is a clean break: v1 stays on the <code>main</code> branch and keeps working; migrate scripts when convenient. This page maps every v1 concept to its v2 home.</p>"},{"location":"migration-v1-to-v2/#setup","title":"Setup","text":"v1 v2 clone repo + <code>pip install -e .</code> (one big package) <code>irm flex.levylab.org/install.ps1 \\| iex</code>, then <code>flex ecosystem use levylab</code> for the lab stack hardcoded DB / Nextcloud / n8n endpoints in code one manifest in the repo's <code>ecosystems/</code> folder: <code>flex ecosystem show</code> after activating <code>levylab</code> secrets in source environment variables (<code>NEXTCLOUD_PASSWORD</code>, <code>ASANA_ACCESS_TOKEN</code>)"},{"location":"migration-v1-to-v2/#imports-and-classes","title":"Imports and classes","text":"v1 v2 <code>from flex.inst.base import Instrument</code> (ZMQ baked in) <code>from flex_protocols import ZMQInstrument</code> (also <code>VISAInstrument</code>, <code>TCPInstrument</code>, <code>SerialInstrument</code>) <code>from flex.inst.levylab.Lockin import Lockin</code> <code>from flex_drivers.levylab.lockin import Lockin</code> <code>inst._send_command(cmd, params)['result']</code> <code>inst.call(cmd, params)</code> \u2014 errors now raise instead of returning <code>None</code> <code>_LABVIEW_CLASS_NAME</code> module constant <code>lv_class</code> class attribute; <code>flex_drivers.levylab.lvclass_registry()</code> derives the CESession lookup from it <code>flex.inst.levylab.insttypes.*</code> empty mixins <code>flex_drivers.levylab.capabilities</code> protocols (real methods; <code>exp.get(Temperature)</code>) <code>from flex.exp.experiment import Experiment</code> <code>from flex import Experiment</code> <code>from flex.exp.CESession import CESession</code> <code>from flex import CESession</code> <code>flex.db.FLEXDB</code> (raw SQL, hardcoded host) metadata store from config: SQLite default, <code>[db] backend = \"postgres\"</code> for the lab DB <code>flex.tdms.flexTDMS.write_tdms</code> <code>[data] writer = \"tdms\"</code> \u2014 every measurement writes TDMS automatically <code>flex.nextcloud</code> <code>[storage] backend = \"nextcloud\"</code> \u2014 files upload on measurement finish <code>flex.exp.dbexptoAsana.trigger_n8n_dbexptoAsana()</code> (n8n) / <code>flex.asana.Asana</code> (hardcoded-token stub) <code>[comms] backend = \"asana\"</code> \u2014 <code>flex-asana</code> talks to the Asana API directly, token from <code>ASANA_ACCESS_TOKEN</code> <code>flex.exp.users</code> hardcoded <code>Literal</code> list any user string; lab-side validation can hook <code>experiment.start</code> <code>flex.exp.script_to_db.CellLogger</code> built into <code>Experiment</code> (<code>cell_log=True</code>), stored in <code>flex_cells</code>"},{"location":"migration-v1-to-v2/#method-names","title":"Method names","text":"<p>Driver methods are now snake_case; the wire protocol is unchanged: <code>setAO_Amplitude(...)</code> \u2192 <code>set_ao_amplitude(...)</code> (still sends <code>setAO_Amplitude</code>).</p>"},{"location":"migration-v1-to-v2/#behavior-changes-worth-knowing","title":"Behavior changes worth knowing","text":"<ul> <li>JSON-RPC errors raise <code>ZMQInstrumentError</code> (v1 silently returned <code>None</code>).</li> <li>A ZMQ timeout no longer wedges the connection \u2014 the socket is reset and   the next call works; you get a <code>TimeoutError</code> you can catch.</li> <li>Ctrl-C during a <code>Scan</code> is safe: the data file is finalized, the   measurement is marked <code>aborted</code>, and <code>on_abort</code> cleanups run.</li> <li>Experiment/measurement IDs gained a random suffix   (<code>20260709153053-bee7</code>) so two starts in the same second cannot collide.</li> <li>Metadata store failures never kill a measurement \u2014 they are logged and   the experiment continues (opt into strictness with <code>[exp] strict_metadata = true</code>).</li> <li>The five <code>PPMS*</code> classes are one <code>PPMS</code> (they only differed in address);   pass the address, or define stations in the config.</li> </ul>"},{"location":"migration-v1-to-v2/#not-carried-over-deliberately","title":"Not carried over (deliberately)","text":"<ul> <li>The v1 <code>flex.asana.Asana</code> file contains a live, hardcoded Asana token \u2014   revoke it (its v2 replacement reads <code>ASANA_ACCESS_TOKEN</code> from the   environment instead; see the row above).</li> <li><code>flex.lv</code> LabVIEW call helpers, <code>exp/pund.py</code> (experiment logic, not   framework \u2014 rewrite as a <code>Scan</code> script), <code>exp/experiment_bak</code>.</li> <li>Newport/Ophir/Sphere drivers that require .NET or COM \u2014 tracked in   <code>packages/flex-drivers/DEFERRED.md</code>.</li> </ul>"},{"location":"migration-v1-to-v2/#postgres-note","title":"Postgres note","text":"<p><code>PostgresStore</code> creates the v2 core schema as <code>flex_</code>-prefixed tables (<code>flex_experiments</code>, <code>flex_measurements</code>, <code>flex_notes</code>, <code>flex_cells</code>, <code>flex_logs</code>, <code>flex_instruments</code>) and does not touch the v1 <code>exp</code> / <code>meas</code> / <code>cell_log</code> tables. Migrating that historical data (or pointing v2 at the same tables) is a coordinated one-off step \u2014 plan it with the lab before switching production.</p>"},{"location":"quickstart/","title":"Quickstart","text":""},{"location":"quickstart/#install","title":"Install","text":"<pre><code>irm flex.levylab.org/install.ps1 | iex\n</code></pre> <p>This brings in the six default packages (<code>flex-core</code>, <code>flex-protocols</code>, <code>flex-db</code>, <code>flex-datatypes</code>, <code>flex-exp</code>, <code>flex-drivers</code>). No configuration is needed \u2014 data goes to HDF5 files and a SQLite database under your user data directory. (Developing FLEX itself, or on another OS? Install editable from a clone \u2014 see the README's Development section.)</p>"},{"location":"quickstart/#your-first-experiment-no-hardware-needed","title":"Your first experiment (no hardware needed)","text":"<pre><code>from flex import Experiment, Scan, sweep, SimulatedInstrument\nimport numpy as np\n\nwith Experiment(\"jane\") as exp:\n    sim = exp.add(SimulatedInstrument, \"demo\")\n    gate = sim.add_sim_parameter(\"gate\", unit=\"V\")\n    x = sim.add_sim_parameter(\"x\", initial=0.5, unit=\"V\")\n\n    Scan(sweep(gate, np.linspace(0, 1, 101), delay=0.01)) \\\n        .measure(sim.parameters[\"x\"]) \\\n        .on_abort(lambda: gate(0)) \\\n        .run(exp, name=\"gate sweep\")\n</code></pre> <p>What happened:</p> <ul> <li>an experiment record was created (id, user, start/end time, instruments),</li> <li>the scan swept <code>gate</code>, read <code>x</code> at every point, and wrote both columns \u2014   with units \u2014 to an HDF5 file,</li> <li>a log file captured everything, and Jupyter cells are recorded as notes,</li> <li>if you had pressed Ctrl-C, the file would have been finalized, the   measurement marked aborted, and the <code>on_abort</code> cleanup would have run.</li> </ul> <p>Browse it:</p> <pre><code>flex experiments\nflex measurements &lt;experiment-id&gt;\n</code></pre>"},{"location":"quickstart/#a-real-instrument","title":"A real instrument","text":"<p>Any VISA instrument works the same way \u2014 drivers are one inheritance:</p> <pre><code>from flex_protocols import VISAInstrument\n\nclass Keithley2400(VISAInstrument):\n    def __init__(self, name=\"k2400\", resource=\"GPIB0::24::INSTR\"):\n        super().__init__(name, resource)\n        self.voltage = self.add_parameter(\n            \"voltage\", get_cmd=\"SOUR:VOLT?\", set_cmd=\"SOUR:VOLT {}\",\n            get_parser=float, unit=\"V\",\n        )\n\nwith Experiment(\"jane\") as exp:\n    k = exp.add(Keithley2400, resource=\"GPIB0::24::INSTR\")\n    Scan(sweep(k.voltage, np.linspace(0, 1, 51), delay=0.1)) \\\n        .measure(current=lambda: k.query(\"MEAS:CURR?\")) \\\n        .run(exp, name=\"IV\")\n</code></pre> <p><code>flex new driver MyInstrument --protocol visa</code> scaffolds this for you.</p>"},{"location":"quickstart/#measurements-without-scans","title":"Measurements without scans","text":"<p>For full control, drive the measurement yourself:</p> <pre><code>with exp.measurement(\"noise vs time\") as m:\n    m.register(k.voltage)              # read on every point, with units\n    for _ in range(1000):\n        m.read_point(t=time.time())    # registered params + your columns\n    m.add_array(\"spectrum\", psd)       # free-form arrays\n    m.add_note(\"AC line filter engaged\")\n</code></pre>"},{"location":"quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Build an ecosystem \u2014 configure your lab's database,   storage, data format, and hooks in one file.</li> <li>LevyLab guide \u2014 <code>CESession</code> and the Instrument Framework.</li> <li>Dashboard \u2014 manage packages and browse experiments   in the browser (<code>python -m flex dashboard</code>).</li> </ul>"},{"location":"concepts/architecture/","title":"Architecture","text":"<p>FLEX is a monorepo of small packages. One package, <code>flex-core</code>, holds the base classes and services; everything else plugs into it through a static JSON catalog. There is no plugin framework, no entry-points, no registration side effects \u2014 a \"plugin\" is a plain dict a package exports.</p>"},{"location":"concepts/architecture/#the-packages","title":"The packages","text":"Package What it is Default <code>flex-core</code> Instrument model, data &amp; metadata services, package manager, dashboard, CLI yes <code>flex-protocols</code> <code>VISAInstrument</code>, <code>TCPInstrument</code>, <code>SerialInstrument</code>, <code>ZMQInstrument</code> base classes yes <code>flex-db</code> Metadata database backends: SQLite (default), PostgreSQL yes <code>flex-exp</code> <code>Experiment</code>, <code>Measurement</code>, <code>Scan</code>, lab sessions (<code>CESession</code>) yes <code>flex-datatypes</code> HDF5 (default) and TDMS (LabVIEW-compatible) data writers yes <code>flex-drivers</code> Instrument drivers, by vendor (including LevyLab, over ZMQ) yes <code>flex-nextcloud</code> Nextcloud file storage opt-in <code>flex-asana</code> Asana comms backend: a task per experiment opt-in <p>The installer gives you the six default packages. Opt-in packages arrive via <code>flex install</code>, <code>flex enable &lt;driver&gt;</code>, or activating an ecosystem that lists them.</p>"},{"location":"concepts/architecture/#what-lives-in-flex-core","title":"What lives in flex-core","text":"<p>Everything other packages build on:</p> <ul> <li>Instrument model \u2014 the <code>Instrument</code> base class and <code>Parameter</code>   (see Instruments &amp; drivers).</li> <li>Data interfaces \u2014 <code>DataWriter</code> (file formats) and <code>StorageBackend</code>   (where finished files live), plus the built-in <code>LocalStorage</code>.</li> <li>Metadata interface \u2014 the <code>MetadataStore</code> ABC and its record dataclasses   (<code>ExperimentRecord</code>, <code>MeasurementRecord</code>, <code>NoteRecord</code>, <code>CellRecord</code>,   <code>LogEntryRecord</code>, <code>InstrumentRecord</code> \u2014 see   Metadata records).</li> <li>EventBus \u2014 small synchronous pub/sub for lifecycle hooks   (see Experiments &amp; data).</li> <li>Ecosystem configuration \u2014 the <code>FlexConfig</code> model and its   resolution rules (see Ecosystems &amp; stations).</li> <li>Package manager, dashboard, CLI \u2014 thin UIs over the above.</li> </ul> <p><code>flex-core</code> has no hardware, database, or file-format dependencies. Those live in the packages that implement them.</p>"},{"location":"concepts/architecture/#how-packages-plug-in","title":"How packages plug in","text":"<p>The catalog (<code>packages/flex-core/src/flex/pkgmanager/catalog.json</code>) describes every official package: group, summary, whether it is a default, which component names it provides, and \u2014 the key part \u2014 a <code>registries</code> entry per component group pointing at a dict the package exports:</p> <pre><code>\"flex-datatypes\": {\n  \"provides\": { \"writer\": [\"hdf5\", \"tdms\"] },\n  \"registries\": { \"writer\": \"flex_datatypes:WRITERS\" }\n}\n</code></pre> <p><code>flex_datatypes.WRITERS</code> is a plain module-level dict:</p> <pre><code>WRITERS: dict[str, str] = {\n    \"hdf5\": \"flex_datatypes.hdf5:HDF5Writer\",\n    \"tdms\": \"flex_datatypes.tdms:TDMSWriter\",\n}\n</code></pre> <p>Same pattern everywhere: <code>flex_drivers:CATALOG</code> for drivers, <code>flex_db:DB_BACKENDS</code> for metadata stores, <code>flex.data:STORAGE</code> and <code>flex_nextcloud:STORAGE</code> for storage backends.</p> <p>A lab extends the catalog without editing anything installed: a <code>catalog.local.json</code> next to the active ecosystem config is merged on top of the built-in one (per-package, by name). That is how a private driver package becomes visible to <code>flex list</code>, <code>flex install</code>, and the dashboard \u2014 see Build an ecosystem.</p>"},{"location":"concepts/architecture/#name-resolution","title":"Name resolution","text":"<p>Configuration refers to components by short name; <code>flex.components</code> turns the name into a class in three steps:</p> <pre><code>[data] writer = \"hdf5\"                     # config: a short name\n        \u2502\n        \u25bc\ncatalog \"registries\" \u2192 flex_datatypes:WRITERS   # which dicts to consult\n        \u2502\n        \u25bc\nWRITERS[\"hdf5\"] \u2192 \"flex_datatypes.hdf5:HDF5Writer\"   # a \"module:Class\" ref\n        \u2502\n        \u25bc\nimport flex_datatypes.hdf5; HDF5Writer     # the class, instantiated with the\n                                           # section's remaining options\n</code></pre> <p>Registries whose providing package is not installed are skipped. If a name resolves to nothing, the error names the official package that provides it (\"Install it with: flex install flex-datatypes\"). Driver names (<code>\"levylab.lockin\"</code>) resolve exactly the same way through the <code>drivers</code> group.</p>"},{"location":"concepts/architecture/#installation","title":"Installation","text":"<p>None of these packages are on PyPI. <code>flex install</code> (used by <code>flex ecosystem use</code> and the dashboard) delegates to the environment's installer \u2014 <code>uv pip</code> in a uv-managed venv, otherwise <code>python -m pip</code> \u2014 and picks the source per package:</p> <ul> <li>inside a clone of this repo (a uv workspace), packages install editable   from <code>packages/&lt;name&gt;</code>;</li> <li>everywhere else, from GitHub:   <code>git+https://github.com/levylabpitt/flex.git@v2#subdirectory=packages/&lt;name&gt;</code>   (branch overridable with <code>$FLEX_SOURCE_REF</code>).</li> </ul> <p>Extras pass through: <code>flex-db[postgres]</code> installs the base package with its PostgreSQL dependencies.</p>"},{"location":"concepts/dashboard/","title":"Dashboard","text":"<p>The dashboard is the CLI's functionality in the browser: a single-page UI served by a small FastAPI app in <code>flex-core</code>. All logic lives in the core services; the dashboard only exposes them.</p> <pre><code>python -m flex dashboard            # http://127.0.0.1:8756\n</code></pre> <p><code>--host</code> and <code>--port</code> override the defaults. The Exit button in the header stops the server (you can also Ctrl-C the terminal).</p> <p>Note</p> <p>Use <code>python -m flex dashboard</code>, not the bare <code>flex dashboard</code> console script. On Windows, <code>flex.exe</code> stays open for the life of the server and holds a file lock on itself; installing or enabling anything through the Integrations/Drivers tabs then fails because the installer can't rewrite its own locked launcher. <code>python -m flex</code> runs the same CLI without that lock.</p>"},{"location":"concepts/dashboard/#tabs","title":"Tabs","text":"<ul> <li>Ecosystem (home) \u2014 the discovered ecosystem manifests with the active   one marked; an Activate button per manifest, or activate any manifest   by path. \"Advanced: raw configuration\" edits the active config TOML   directly, validated against the config schema before saving.</li> <li>Integrations \u2014 the official package list with install/uninstall   buttons.</li> <li>Drivers \u2014 every known driver, searchable; enable/disable each, and   Probe an enabled driver (connect and query its identity).</li> <li>Experiments \u2014 recent experiment records; click one for its   measurements, data-file pointers, and notes.</li> </ul>"},{"location":"concepts/dashboard/#security","title":"Security","text":"<p>The dashboard is a localhost tool. It binds to <code>127.0.0.1</code> by default, and every request's <code>Host</code> header is checked: anything other than <code>localhost</code>/<code>127.0.0.1</code>/<code>[::1]</code> is rejected with 403, which blocks DNS rebinding and cross-site requests against the local server.</p>"},{"location":"concepts/ecosystems/","title":"Ecosystems &amp; stations","text":"<p>This page explains the concepts; Build an ecosystem is the how-to, with a complete annotated manifest.</p>"},{"location":"concepts/ecosystems/#the-hierarchy","title":"The hierarchy","text":"<p>Three levels, from lab-wide to per-device:</p> <ul> <li>An ecosystem is a lab's shared setup in one TOML manifest: the packages   to install plus the service configuration everyone shares \u2014 database,   storage, data format, hooks. One file per lab, versioned in the repo.</li> <li>A station is one bench's instrument set: a <code>[stations.&lt;name&gt;]</code> block   mapping instrument names to drivers and addresses. Stations are typically   added locally (edited into the active config on that machine), not   shipped lab-wide \u2014 your bench's addresses are not your neighbor's.</li> <li>An instrument entry names a driver and an address:</li> </ul> <pre><code>[stations.cryo1.instruments.lockin]\ndriver = \"srs.sr7270\"\naddress = \"USB0::0x0A2D::0x001B::12345::RAW\"\n</code></pre> <p>Extra keys in an instrument entry pass straight through to the driver's constructor as keyword arguments.</p>"},{"location":"concepts/ecosystems/#where-manifests-live","title":"Where manifests live","text":"<p>Manifests are discovered dynamically from two places; nothing is hardcoded:</p> <ol> <li><code>default.toml</code>, bundled inside <code>flex-core</code> itself    (<code>flex.pkgmanager.ecosystems</code>). It is the explicit form of running with no    configuration at all: SQLite, HDF5, local files.</li> <li>The repo's top-level <code>ecosystems/</code> folder (found when running from a    workspace checkout). This is where lab manifests like <code>levylab.toml</code> live \u2014    it is repo content, not package content, so a fork can delete, replace,    or add manifests without touching <code>flex-core</code>.</li> </ol> <p>Later locations override earlier ones by name. <code>flex ecosystem use &lt;name&gt;</code> resolves a bare name against these folders; a path to any <code>.toml</code> file works too.</p>"},{"location":"concepts/ecosystems/#what-activation-does-and-does-not-do","title":"What activation does \u2014 and does not do","text":"<p><code>flex ecosystem use &lt;manifest&gt;</code> does exactly two things:</p> <ol> <li>installs the packages listed under <code>[ecosystem] packages</code> (see    Architecture for where they come from),</li> <li>copies the manifest to the active-config location, making it the    configuration every subsequent <code>Experiment</code>, CLI command, and dashboard    session resolves.</li> </ol> <p>It deliberately does not configure your bench. <code>levylab.toml</code> ships no <code>[drivers]</code> or <code>[stations.*]</code> sections: which drivers are enabled and which instruments live at your bench are per-station concerns. You add those locally \u2014 <code>flex enable &lt;driver&gt;</code>, the dashboard's raw-config editor, or editing the file directly \u2014 on top of the activated lab config.</p>"},{"location":"concepts/ecosystems/#configuration-resolution","title":"Configuration resolution","text":"<p>At runtime the active configuration is found by a fixed precedence: explicit path \u2192 <code>$FLEX_CONFIG</code> \u2192 <code>./flex.toml</code> \u2192 the activated config in the user data directory \u2192 built-in defaults. The full list, and what each level is for, is in Build an ecosystem. <code>./flex.toml</code> outranking the activated config is the escape hatch: a project folder can carry its own complete configuration without touching the machine-wide one.</p> <p>Every section of the manifest is optional; short component names in it (\"postgres\", \"tdms\", \"nextcloud\") resolve to classes through the package catalog \u2014 see Architecture.</p>"},{"location":"concepts/ecosystems/#stations-at-runtime","title":"Stations at runtime","text":"<p><code>exp.load_station(\"cryo1\")</code> instantiates every instrument in the block \u2014 resolving each <code>driver</code> name by catalog, passing the address and extra keys \u2014 and registers them on the experiment. Each <code>driver</code> must be enabled first (<code>flex enable &lt;driver&gt;</code>) \u2014 see Instruments &amp; drivers. With <code>[lab] station</code> set (or only one station defined), the name argument is optional. <code>flex instruments --probe</code> test-connects the same entries from the shell.</p> <p>The LevyLab ecosystem replaces station config with the Configure Experiments VI: see CESession.</p>"},{"location":"concepts/experiments/","title":"Experiments &amp; data","text":"<p>Three objects, three scopes:</p> <ul> <li>Experiment \u2014 a session: instruments, services, one log file, one   metadata record.</li> <li>Measurement \u2014 one data file plus one metadata record, always used as a   context manager.</li> <li>Scan \u2014 an autonomous loop that runs inside a measurement.</li> </ul>"},{"location":"concepts/experiments/#experiment","title":"Experiment","text":"<p><code>Experiment</code> is a context manager. On construction it loads the active configuration, builds its services from it (metadata store, storage backend, event bus with configured hooks), records an experiment-start row, and opens a per-experiment log file. On exit it records the end time, fires <code>experiment.end</code>, releases the services, and closes every registered instrument.</p> <pre><code>with Experiment(\"jane\") as exp:\n    lockin = exp.add(SR7270, \"lockin\", \"USB0::...\")   # construct + register\n    exp.lockin                                        # attribute access by name\n</code></pre> <p>IDs are sortable and collision-safe: <code>YYYYMMDDHHMMSS-&lt;4 hex chars&gt;</code>.</p> <p>Inside Jupyter, every executed cell is recorded as a row in <code>flex_cells</code> (disable with <code>cell_log=False</code>), so the code that produced a dataset is always recoverable \u2014 including its execution count and whether it raised.</p> <p>Instruments can also come from configuration: <code>exp.load_station(\"cryo1\")</code> instantiates everything in the config's <code>[stations.cryo1]</code> block \u2014 see Ecosystems &amp; stations.</p> <p>In Jupyter or VS Code's Interactive Window, every <code>Experiment</code> shows a live summary card (id, user, instruments) that updates as instruments are added and again when the experiment ends \u2014 since <code>with Experiment(...) as exp:</code> never makes <code>exp</code> the last expression of a cell, <code>_repr_html_</code> alone would never render it, so <code>flex.display.auto_display</code>/<code>refresh_display</code> push it explicitly instead. Routine logging is quieted to WARNING in interactive sessions for the same reason (see <code>flex.log.enable_console</code>) so the card stays the signal, not log lines. <code>CESession</code> (below) is just an <code>Experiment</code> subclass with a richer card \u2014 nothing CESession-specific about the mechanism.</p>"},{"location":"concepts/experiments/#measurement","title":"Measurement","text":"<p><code>exp.measurement(name)</code> returns a <code>Measurement</code> context manager owning one data file. Two recording styles, freely mixed: push rows yourself with <code>add_row(**values)</code>, or <code>register()</code> parameters/callables and call <code>read_point(**setpoints)</code> to read them all and record the row. Columns are fixed by the first data point; <code>add_array()</code> stores free-form arrays; <code>add_note()</code> attaches a note to this measurement. See the Quickstart for an example.</p> <p>On entry, the writer (from <code>[data] writer</code>, overridable per measurement) opens the file with embedded metadata \u2014 experiment/measurement IDs, name, user, start time, and a full instrument snapshot \u2014 and a measurement-start row is recorded.</p> <p>On exit \u2014 normal or not \u2014 in this order:</p> <ol> <li>the writer is closed (a failure is logged; the file may be incomplete),</li> <li>the storage backend finalizes the file (remote backends upload here; a    failure is logged and the record falls back to the local path),</li> <li>the metadata record is updated with end time, file pointer, and an    <code>aborted</code> flag if the block was left through an exception,</li> <li><code>measurement.end</code> (or <code>measurement.abort</code>) fires.</li> </ol> <p>Writers always write to a local path; remote storage acts only in step 2, so a network failure can never corrupt a live measurement.</p>"},{"location":"concepts/experiments/#scan-and-sweep","title":"Scan and sweep","text":"<p><code>sweep(target, values, delay=...)</code> describes one axis \u2014 a <code>Parameter</code> or any one-argument callable, its values, and pacing (plus optional <code>before</code>/<code>after</code> per-pass callbacks). <code>Scan(*axes)</code> iterates them as a grid (first axis outermost) and records a row per point:</p> <pre><code>Scan(sweep(gate, np.linspace(0, 1, 101), delay=0.01)) \\\n    .measure(lockin.x, lockin.y) \\\n    .each(update_plot) \\\n    .on_abort(lambda: gate(0)) \\\n    .run(exp, name=\"gate sweep\")\n</code></pre> <p><code>run()</code> wraps everything in a new measurement. On any interruption \u2014 including Ctrl-C \u2014 the <code>on_abort</code> callbacks run first (errors in one are logged, the rest still run), then the measurement context exits: file finalized, record marked aborted, <code>measurement.abort</code> fired, and only then does the interrupt propagate.</p>"},{"location":"concepts/experiments/#where-files-land","title":"Where files land","text":"<p>With no configuration, everything goes under the per-user data directory (<code>platformdirs</code>, e.g. <code>%LOCALAPPDATA%/flex/data</code> on Windows); set <code>[data] root</code> to move it. Local storage lays files out by year:</p> <pre><code>{root}/{year}/{experiment_id}/experiment.log\n{root}/{year}/{experiment_id}/{measurement_id}.h5\n</code></pre> <p>(<code>year</code> is the first four characters of the experiment ID.) Remote backends like Nextcloud decide their own final location; the metadata record always holds the canonical <code>FilePointer</code>.</p>"},{"location":"concepts/experiments/#metadata-records","title":"Metadata records","text":"<p>The metadata store (from <code>[db] backend</code>; SQLite by default) keeps six <code>flex_</code>-prefixed tables, normalized one entity per table:</p> <ul> <li>flex_experiments \u2014 id, user, name, start/end, ecosystem, station, host,   flex version, full config snapshot.</li> <li>flex_measurements \u2014 id, experiment, name, start/end, aborted, writer   format, row count, file pointer (uri/backend/size).</li> <li>flex_instruments \u2014 one row per <code>exp.add()</code>/<code>add_instrument()</code> call:   name, driver class, address, options.</li> <li>flex_notes \u2014 free-text notes (<code>exp.note(...)</code>).</li> <li>flex_cells \u2014 one row per executed Jupyter cell (source, execution   count, success/error) \u2014 see <code>cell_log</code> above.</li> <li>flex_logs \u2014 an opt-in mirror of the <code>flex</code> logger namespace; empty   unless <code>[logs] mirror_to_db = true</code> (default level <code>\"WARNING\"</code>, override   with <code>[logs] level</code>). Kept separate from <code>flex_notes</code>/<code>flex_cells</code> since   it's off by default and can be high-volume.</li> </ul> <p>The <code>flex_</code> prefix keeps this schema unambiguous in a database that may also hold unrelated or legacy tables. Browse the tables with <code>flex experiments</code> / <code>flex measurements &lt;id&gt;</code> or the dashboard.</p> <p>Store failures never kill a run: if the store is unavailable or a write fails, it is logged and the experiment continues. Opt into hard failures with <code>[exp] strict_metadata = true</code>.</p>"},{"location":"concepts/experiments/#comms","title":"Comms","text":"<p><code>[comms] backend</code> (<code>\"none\"</code> by default) notifies an external system of the experiment lifecycle \u2014 currently just <code>flex-asana</code>'s <code>\"asana\"</code> (one Asana task per experiment, stamped with the start time and assigned to the user \u2014 unassigned if the handle can't be resolved, never a hard failure; the end time fills in when the experiment ends \u2014 see its package docstring for the environment variables it reads). <code>Experiment</code> builds the backend and calls it wrapped in try/except, same as the metadata store: a missing token, unreachable API, or misconfigured project never breaks a run, only logs a warning. Pass <code>Experiment(..., notify=False)</code> to skip it for one run without touching the ecosystem config.</p> <p>If <code>flex-asana</code> is installed and <code>python -m flex_asana.update_users</code> has been run at least once (regenerates a <code>Literal</code> of workspace handles from Asana \u2014 rerun it when people join/leave), <code>Experiment(user=...)</code> and <code>CESession(user=...)</code> get IDE autocomplete for known handles; otherwise <code>user</code> is (and always accepts) a plain string.</p>"},{"location":"concepts/experiments/#hooks-and-events","title":"Hooks and events","text":"<p>Every lifecycle transition is emitted on the experiment's <code>EventBus</code>: <code>experiment.start</code>, <code>experiment.end</code>, <code>measurement.start</code>, <code>measurement.end</code>, <code>measurement.abort</code>, <code>note.added</code>, <code>instrument.added</code>.</p> <p>Hooks subscribe from configuration \u2014 any callable <code>fn(event, experiment, **payload)</code> referenced by a <code>\"module:function\"</code> string:</p> <pre><code>[hooks]\non_experiment_end = [\"some_package.hooks:notify\"]\n</code></pre> <p>Keys may use the <code>on_&lt;event with _&gt;</code> form shown or the bare event name. Subscriber exceptions are logged and swallowed: a broken webhook must never break a running experiment. A hook that fails to load is also only a warning. This is the general-purpose extension point; official integrations like Asana use the more specific <code>[comms]</code> layer instead (above), matching how <code>[db]</code>/<code>[data]</code>/<code>[storage]</code> work rather than requiring hand-wired hooks.</p>"},{"location":"concepts/experiments/#cesession","title":"CESession","text":"<p><code>CESession</code> (LevyLab-specific, in <code>flex-exp</code>) is an <code>Experiment</code> subclass that gets its instruments from the LabVIEW Configure Experiments VI file instead of code or config: it reads <code>Control Experiment.json</code>, matches each entry's LabVIEW class against <code>lvclass_registry()</code> (see Instruments &amp; drivers), and connects a driver per instrument. <code>exp.update()</code> re-reads the file and connects anything added while running.</p> <p>Its card (inherited from <code>Experiment</code>, see above) additionally shows device, station, and wiring. Pass <code>verbose=True</code> to also print a line as each instrument connects \u2014 useful while debugging a new station.</p> <p><code>CESession</code> and <code>load_station()</code> are two independent mechanisms for the same job \u2014 populating <code>exp.instruments</code>. One is driven by the lab's LabVIEW tooling, the other by <code>[stations.*]</code> config; everything downstream (measurements, scans, records, hooks) is identical. Usage examples are in the LevyLab guide.</p>"},{"location":"concepts/instruments/","title":"Instruments &amp; drivers","text":"<p>A FLEX driver is an ordinary Python class. It inherits a connection from a protocol base class, declares its knobs as parameters, and adds plain methods for everything else. Nothing needs to be registered before you can use it.</p>"},{"location":"concepts/instruments/#the-instrument-base-class","title":"The Instrument base class","text":"<p><code>flex.instrument.Instrument</code> holds everything that does not depend on how the instrument is connected: name, per-instrument logger (<code>inst.&lt;name&gt;</code>), the parameter registry, <code>snapshot()</code>, <code>idn()</code>, and context-manager lifecycle (<code>with ... :</code> closes the connection on exit). Protocol classes add the wire: <code>query</code>/<code>write</code> for text protocols, <code>call</code> for JSON-RPC.</p>"},{"location":"concepts/instruments/#parameters","title":"Parameters","text":"<p><code>add_parameter</code> registers a named, unit-carrying get/set handle:</p> <pre><code>def add_parameter(self, name, *, get_cmd=None, set_cmd=None,\n                  getter=None, setter=None, get_parser=None,\n                  unit=\"\", vals=None, doc=\"\")\n</code></pre> <ul> <li><code>get_cmd</code> / <code>set_cmd</code> are command templates sent through the instrument's   <code>query</code>/<code>write</code>; <code>set_cmd</code> is formatted with the value   (<code>\"SOUR:VOLT {}\"</code>), <code>get_parser</code> converts the reply string (<code>float</code>).</li> <li><code>getter</code> / <code>setter</code> are arbitrary callables, for instruments whose methods   already exist. Give one style or the other, not both.</li> <li><code>vals</code> is an optional validator, checked on every set.</li> </ul> <p>A parameter is callable \u2014 no value reads, one value writes:</p> <pre><code>k.voltage = k.add_parameter(\"voltage\", get_cmd=\"SOUR:VOLT?\",\n                            set_cmd=\"SOUR:VOLT {}\", get_parser=float, unit=\"V\")\nk.voltage(0.5)     # set\nk.voltage()        # get -&gt; 0.5\n</code></pre> <p>Sweeps and measurements consume parameters directly: the column name and unit in the data file come from the parameter (see Experiments &amp; data).</p> <p>Two validators exist: <code>Numbers(min, max)</code> accepts any real number in range \u2014 including numpy scalars, excluding <code>bool</code> \u2014 and <code>Enum(*values)</code> accepts a fixed set. Both raise on <code>set</code> before anything reaches the hardware.</p>"},{"location":"concepts/instruments/#protocol-base-classes","title":"Protocol base classes","text":"<p><code>flex-protocols</code> provides one base per connection type:</p> Class Connection Talk to it with <code>VISAInstrument</code> GPIB / USB / RS-232 via VISA / TCPIP INSTR <code>query(cmd)</code>, <code>write(cmd)</code> <code>TCPInstrument</code> raw TCP socket, line-based text <code>query(cmd)</code>, <code>write(cmd)</code> <code>SerialInstrument</code> COM port <code>query(cmd)</code>, <code>write(cmd)</code> <code>ZMQInstrument</code> JSON-RPC 2.0 over a ZMQ REQ socket <code>call(method, params)</code> <p>All four connect eagerly in <code>__init__</code> and raise if the connection cannot be opened \u2014 constructing a driver instance means you are connected. All take a <code>timeout</code> in seconds and accept <code>**kwargs</code> passed up to <code>Instrument</code>.</p>"},{"location":"concepts/instruments/#zmqinstrument-specifics","title":"ZMQInstrument specifics","text":"<p><code>ZMQInstrument</code> speaks the LevyLab Instrument-Framework dialect of JSON-RPC (but works with any JSON-RPC-over-ZMQ endpoint). On connect it sends an <code>ACK</code> to verify the endpoint (disable with <code>connect_check=False</code>).</p> <ul> <li><code>call(method, params)</code> returns the JSON-RPC <code>result</code>.</li> <li>A JSON-RPC <code>error</code> response raises <code>ZMQInstrumentError</code> (carries <code>.code</code>   and <code>.data</code>).</li> <li>A missed reply raises <code>TimeoutError</code> and resets the REQ socket \u2014 a REQ   socket that missed its reply is otherwise stuck forever, so the next call   works again.</li> <li><code>idn()</code> and <code>help()</code> wrap the IF built-ins <code>IDN</code> and <code>HELP</code>.</li> </ul>"},{"location":"concepts/instruments/#driver-packages-and-the-catalog","title":"Driver packages and the catalog","text":"<p>Drivers live in vendor folders inside <code>flex_drivers</code> (<code>srs/</code>, <code>colby/</code>, <code>rotrics/</code>, <code>levylab/</code>). The package exports one plain dict:</p> <pre><code>CATALOG: dict[str, str] = {\n    \"srs.sr7270\": \"flex_drivers.srs.sr7270:SR7270\",\n    \"levylab.lockin\": \"flex_drivers.levylab.lockin:Lockin\",\n    ...\n}\n</code></pre> <p>Importing <code>flex_drivers</code> imports no driver module (and no zmq/pyvisa); the values are <code>\"module:Class\"</code> references resolved lazily.</p> <p>Two ways to get a driver class:</p> <ul> <li>Direct import \u2014 always works. A driver is just a class:   <code>from flex_drivers.srs.sr7270 import SR7270</code>. No installation state, no   enablement, nothing gates imports.</li> <li>By name, through the catalog: station configs (<code>driver =   \"srs.sr7270\"</code>), <code>flex instruments --probe</code>, and the dashboard resolve   driver names via <code>CATALOG</code> \u2014 and require the name to be enabled first.</li> </ul> <p><code>flex enable &lt;name&gt;</code> adds the name to <code>[drivers] enabled</code> in the active config (installing the parent package first if needed). Every name-based resolution (<code>load_station()</code>, <code>--probe</code>, the dashboard) refuses a driver that isn't enabled, even if its package is installed \u2014 a deliberate gate on the config-driven path, not on imports: direct imports and <code>CESession</code> (which only ever connects instruments the Configure Experiments file says are physically wired up) are unaffected either way.</p>"},{"location":"concepts/instruments/#levylab-drivers-and-lv_class","title":"LevyLab drivers and lv_class","text":"<p>Drivers for LevyLab Instrument-Framework apps additionally carry a <code>lv_class</code> class attribute \u2014 the LabVIEW class name the IF app reports:</p> <pre><code>class Lockin(ZMQInstrument):\n    lv_class = \"Instrument.Lockin.lvclass\"\n</code></pre> <p><code>flex_drivers.levylab.lvclass_registry()</code> walks the catalog and builds <code>{LabVIEW class name: \"module:Class\"}</code> from these attributes (plus <code>lv_class_aliases</code> \u2014 the five v1 PPMS variants all map to the one <code>PPMS</code> driver). <code>CESession</code> uses that registry to auto-connect a driver to every instrument in the Configure Experiments VI file \u2014 see the LevyLab guide. A driver with <code>lv_class = None</code> (e.g. <code>TransportServer</code>) is never auto-discovered.</p>"},{"location":"concepts/instruments/#simulatedinstrument","title":"SimulatedInstrument","text":"<p><code>flex.SimulatedInstrument</code> behaves like a text-protocol instrument without hardware: <code>replies</code> maps commands to canned responses, every command sent is recorded in <code>.sent</code>, and <code>add_sim_parameter(name, initial=..., unit=...)</code> creates an in-memory readable/settable parameter. It powers the Quickstart example, tests, and dry runs. For testing ZMQ drivers against a fake IF app, see Write a driver.</p>"},{"location":"reference/cli/","title":"CLI reference","text":"<p>Every command of the <code>flex</code> command-line tool. Global behavior: running <code>flex</code> with no arguments prints help.</p>"},{"location":"reference/cli/#packages-drivers","title":"Packages &amp; drivers","text":"Command Does <code>flex list</code> List official packages: group, installed version, summary. <code>flex list --drivers</code> List drivers instead: parent package, available (package installed), enabled. <code>flex install &lt;pkg&gt;...</code> Install official packages (editable from a repo checkout, else from GitHub \u2014 see Architecture). <code>flex remove &lt;pkg&gt;...</code> Uninstall packages. <code>flex enable &lt;driver&gt;</code> Add a driver to <code>[drivers] enabled</code> in the active config; installs its parent package first if missing. <code>flex disable &lt;driver&gt;</code> Remove a driver from <code>[drivers] enabled</code>."},{"location":"reference/cli/#ecosystems","title":"Ecosystems","text":"Command Does <code>flex ecosystem use &lt;manifest&gt;</code> Activate an ecosystem: install its packages, make it the active config. <code>&lt;manifest&gt;</code> is a file path or a known name (bundled <code>default</code>, or the repo's <code>ecosystems/</code> folder). <code>--no-install</code> skips package installation. <code>flex ecosystem show</code> Print the resolved active configuration and its source file. <code>flex ecosystem validate &lt;manifest&gt;</code> Check a manifest: schema, then whether its db/writer/storage components resolve to installed packages."},{"location":"reference/cli/#records","title":"Records","text":"Command Does <code>flex experiments</code> Browse recorded experiments. <code>--user &lt;name&gt;</code> filters; <code>--last &lt;n&gt;</code> limits (default 20). <code>flex measurements &lt;experiment-id&gt;</code> List an experiment's measurements (times, aborted flag, data file) and note count. <code>flex instruments</code> List instruments configured in the active <code>[stations.*]</code> blocks. <code>--probe</code> connects each one and shows its identity (or the error)."},{"location":"reference/cli/#scaffolding","title":"Scaffolding","text":"Command Does <code>flex new driver &lt;Name&gt;</code> Write a driver skeleton <code>&lt;name&gt;.py</code> in the current directory. <code>--protocol visa\\|tcp\\|serial\\|zmq</code> picks the base class (default <code>visa</code>); <code>--out &lt;dir&gt;</code> changes the destination. Refuses to overwrite. <code>flex new package &lt;name&gt;</code> Create an installable driver-package skeleton (<code>pyproject.toml</code>, <code>src/&lt;module&gt;/__init__.py</code> with a <code>CATALOG</code> dict, <code>tests/</code>). <code>--out &lt;dir&gt;</code> changes the destination. <p>See Write a driver for both in context.</p>"},{"location":"reference/cli/#dashboard-version","title":"Dashboard &amp; version","text":"Command Does <code>python -m flex dashboard</code> Launch the web dashboard. <code>--host</code> (default <code>127.0.0.1</code>), <code>--port</code> (default <code>8756</code>). Use the module form, not <code>flex dashboard</code> \u2014 see the dashboard page for why. <code>flex version</code> Show the version of every installed FLEX package."},{"location":"reference/drivers/","title":"Drivers","text":"<p>Every driver in <code>flex-drivers</code>, generated from the package's live <code>CATALOG</code> at build time. The name column is what goes in a station's <code>driver = ...</code> key and in <code>flex enable</code>.</p>"},{"location":"reference/drivers/#colby","title":"colby","text":"Name Class Summary <code>colby.pdl</code> <code>ColbyPDL</code> Colby Instruments programmable delay line."},{"location":"reference/drivers/#levylab","title":"levylab","text":"Name Class Summary <code>levylab.aerotech</code> <code>Aerotech</code> Aerotech stage driver placeholder (no IF app yet; constructing raises). <code>levylab.cryostation</code> <code>Cryostation</code> Montana Cryostation driver with Temperature capabilities. <code>levylab.krohn_hite</code> <code>KrohnHite7008</code> Krohn-Hite 7008 8-channel amplifier (LevyLab Instrument Framework app). <code>levylab.lockin</code> <code>Lockin</code> Multichannel lock-in amplifier (LevyLab Instrument Framework app). <code>levylab.opticool</code> <code>Opticool</code> Opticool driver with Temperature and Magnet capabilities. <code>levylab.oxford1820</code> <code>Oxford1820</code> Oxford 1820 Magnet subsystem (MNK fridge). <code>levylab.oxford_vrm</code> <code>OxfordVRM</code> Oxford VRM Magnet subsystem (CF900 fridge). <code>levylab.ppms</code> <code>PPMS</code> PPMS driver with Temperature and Magnet capabilities. <code>levylab.tc_cf</code> <code>TC_CF</code> Leiden TC (Z Bridge) on the CF900 fridge. <code>levylab.tc_mnk</code> <code>TC_MNK</code> Leiden TC (AVS47B) on the MNK fridge. <code>levylab.transport_server</code> <code>TransportServer</code> Transport measurement orchestration server (LevyLab IF app)."},{"location":"reference/drivers/#rotrics","title":"rotrics","text":"Name Class Summary <code>rotrics.dexarm</code> <code>DexArm</code> Rotrics DexArm robot arm."},{"location":"reference/drivers/#srs","title":"srs","text":"Name Class Summary <code>srs.sr7270</code> <code>SR7270</code> Driver for the 7270 DSP lock-in amplifier via its RAW USB VISA resource."},{"location":"reference/drivers/SUMMARY/","title":"SUMMARY","text":"<ul> <li>Overview</li> <li>colby.pdl</li> <li>levylab.aerotech</li> <li>levylab.cryostation</li> <li>levylab.krohn_hite</li> <li>levylab.lockin</li> <li>levylab.opticool</li> <li>levylab.oxford1820</li> <li>levylab.oxford_vrm</li> <li>levylab.ppms</li> <li>levylab.tc_cf</li> <li>levylab.tc_mnk</li> <li>levylab.transport_server</li> <li>rotrics.dexarm</li> <li>srs.sr7270</li> </ul>"},{"location":"reference/drivers/colby-pdl/","title":"<code>colby.pdl</code>","text":""},{"location":"reference/drivers/colby-pdl/#flex_drivers.colby.pdl.ColbyPDL","title":"<code>flex_drivers.colby.pdl.ColbyPDL</code>","text":"<p>               Bases: <code>VISAInstrument</code></p> <p>Colby Instruments programmable delay line.</p> <p>On connect the driver reads the stepper rate (<code>RATE?</code>) and re-sends it before each :meth:<code>set_delay</code> unless an explicit rate is given.</p>"},{"location":"reference/drivers/colby-pdl/#flex_drivers.colby.pdl.ColbyPDL.set_delay","title":"<code>set_delay(delay, unit='ns', stepper_rate=None)</code>","text":"<p>Set the delay.</p> <p>Parameters:</p> Name Type Description Default <code>delay</code> <code>float</code> <p>Delay value.</p> required <code>unit</code> <code>str</code> <p>Delay unit string sent to the instrument (default <code>\"ns\"</code>).</p> <code>'ns'</code> <code>stepper_rate</code> <code>float | None</code> <p>Optional stepper rate; defaults to the last rate used (initially the rate read from the instrument at connect).</p> <code>None</code>"},{"location":"reference/drivers/colby-pdl/#flex_drivers.colby.pdl.ColbyPDL.get_delay","title":"<code>get_delay()</code>","text":"<p>Get the current delay (raw instrument reply to <code>DEL?</code>).</p>"},{"location":"reference/drivers/levylab-aerotech/","title":"<code>levylab.aerotech</code>","text":"<p>LabVIEW class: <code>Instrument.Aerotech.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-aerotech/#flex_drivers.levylab.aerotech.Aerotech","title":"<code>flex_drivers.levylab.aerotech.Aerotech</code>","text":"<p>               Bases: <code>ZMQInstrument</code></p> <p>Aerotech stage driver placeholder (no IF app yet; constructing raises).</p>"},{"location":"reference/drivers/levylab-cryostation/","title":"<code>levylab.cryostation</code>","text":"<p>LabVIEW class: <code>instrument.Cryostation.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-cryostation/#flex_drivers.levylab.cryostation.Cryostation","title":"<code>flex_drivers.levylab.cryostation.Cryostation</code>","text":"<p>               Bases: <code>ZMQInstrument</code>, <code>IFTemperatureCommands</code></p> <p>Montana Cryostation driver with Temperature capabilities.</p>"},{"location":"reference/drivers/levylab-cryostation/#flex_drivers.levylab.cryostation.Cryostation.get_temperature","title":"<code>get_temperature(channel=0)</code>","text":"<p>Read a temperature channel.</p> <p>channel 0: Sample Temperature channel 1: Platform Temperature channel 2: Stage 1 Temperature channel 3: Stage 2 Temperature</p>"},{"location":"reference/drivers/levylab-cryostation/#flex_drivers.levylab.cryostation.Cryostation.set_temperature","title":"<code>set_temperature(setpoint, rate=None, *, channel=0)</code>","text":"<p>Override to disable control for this specific hardware.</p>"},{"location":"reference/drivers/levylab-krohn_hite/","title":"<code>levylab.krohn_hite</code>","text":"<p>LabVIEW class: <code>Inst.Krohn-Hite-7008.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-krohn_hite/#flex_drivers.levylab.krohn_hite.KrohnHite7008","title":"<code>flex_drivers.levylab.krohn_hite.KrohnHite7008</code>","text":"<p>               Bases: <code>ZMQInstrument</code></p> <p>Krohn-Hite 7008 8-channel amplifier (LevyLab Instrument Framework app).</p> <p>Channel configuration dicts use the IF key spellings, e.g.::</p> <pre><code>{\"channel\": 1, \"gain\": \"10\", \"input\": \"DIFF\", \"shunt\": \"10M\",\n \"couple\": \"AC\", \"filter\": \"OFF\"}\n</code></pre>"},{"location":"reference/drivers/levylab-krohn_hite/#flex_drivers.levylab.krohn_hite.KrohnHite7008.set_kh_channel","title":"<code>set_kh_channel(config)</code>","text":"<p>Configure several channels at once (IF <code>setAllChannels</code>).</p>"},{"location":"reference/drivers/levylab-krohn_hite/#flex_drivers.levylab.krohn_hite.KrohnHite7008.set_kh_channel_single","title":"<code>set_kh_channel_single(config)</code>","text":"<p>Configure a single channel (IF <code>setChannel</code>).</p>"},{"location":"reference/drivers/levylab-krohn_hite/#flex_drivers.levylab.krohn_hite.KrohnHite7008.get_channel","title":"<code>get_channel(channel)</code>","text":"<p>Read the configuration of one channel (IF <code>getChannel</code>).</p>"},{"location":"reference/drivers/levylab-lockin/","title":"<code>levylab.lockin</code>","text":"<p>LabVIEW class: <code>Instrument.Lockin.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin","title":"<code>flex_drivers.levylab.lockin.Lockin</code>","text":"<p>               Bases: <code>ZMQInstrument</code></p> <p>Multichannel lock-in amplifier (LevyLab Instrument Framework app).</p>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin.get_ao","title":"<code>get_ao(channel)</code>","text":"<p>Read the analog output configuration of <code>channel</code>.</p>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin.get_ai","title":"<code>get_ai(channel)</code>","text":"<p>Read the analog input of <code>channel</code>.</p>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin.set_ao_function","title":"<code>set_ao_function(channel, value)</code>","text":"<p>Set the AO waveform: Sine, Triangle, or Square.</p>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin.set_sampling_mode","title":"<code>set_sampling_mode(value)</code>","text":"<p>Set the sampling mode (IF method <code>setSamplingFsMode</code>).</p>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin.set_sweep","title":"<code>set_sweep(sweep_config)</code>","text":"<p>Configure a sweep.</p> <p>sweep_config format::</p> <pre><code>sweep_config = {\"Sweep Time (s)\": sweep_time,\n                \"Initial Wait (s)\": 2,\n                \"Return to Start\": False,\n                \"Channels\": [{\"Enable?\": True,\n                              \"Channel\": channel,\n                              \"Start\": start,\n                              \"End\": stop,\n                              \"Pattern\": \"Ramp /\",\n                              \"Table\": []},\n                             ]}\n</code></pre>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin.set_ao","title":"<code>set_ao(channel, value)</code>","text":"<p>Canonical DAQ capability alias: set the DC value of an AO channel.</p>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin.get_lockin_result","title":"<code>get_lockin_result(channel, param, ref=1)</code>","text":"<p>Extract one value (e.g. <code>X</code>, <code>Theta</code>, <code>Mean</code>) from <code>getResults</code>.</p>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin.lockin_sweep","title":"<code>lockin_sweep(sweep_config, timeout=10)</code>","text":"<p>Run a sweep and block until it finishes.</p>"},{"location":"reference/drivers/levylab-lockin/#flex_drivers.levylab.lockin.Lockin.set_backgate","title":"<code>set_backgate(bg_channel, bg_target, sweep_rate, initial_wait=1, tol=0.001)</code>","text":"<p>Sweep the backgate voltage to a target value.</p> <p>Parameters:</p> Name Type Description Default <code>bg_channel</code> <code>int</code> <p>Analog output channel used for the backgate.</p> required <code>bg_target</code> <code>float</code> <p>Target backgate voltage in volts (V).</p> required <code>sweep_rate</code> <code>float</code> <p>Sweep rate in seconds per volt (s/V).</p> required <code>initial_wait</code> <code>float</code> <p>Delay before starting the sweep in seconds. Default 1 s.</p> <code>1</code> <code>tol</code> <code>float</code> <p>Voltage tolerance in volts used to determine whether the gate is already at the target voltage. Default 1e-3 V.</p> <code>0.001</code>"},{"location":"reference/drivers/levylab-opticool/","title":"<code>levylab.opticool</code>","text":"<p>LabVIEW class: <code>instrument.OptiCool.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-opticool/#flex_drivers.levylab.opticool.Opticool","title":"<code>flex_drivers.levylab.opticool.Opticool</code>","text":"<p>               Bases: <code>ZMQInstrument</code>, <code>IFTemperatureCommands</code>, <code>IFMagnetCommands</code></p> <p>Opticool driver with Temperature and Magnet capabilities.</p>"},{"location":"reference/drivers/levylab-opticool/#flex_drivers.levylab.opticool.Opticool.get_lhe_level","title":"<code>get_lhe_level()</code>","text":"<p>Read the liquid-helium level (IF <code>getLHeLevel</code>).</p>"},{"location":"reference/drivers/levylab-oxford1820/","title":"<code>levylab.oxford1820</code>","text":"<p>LabVIEW class: <code>Instrument.Oxford1820.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-oxford1820/#flex_drivers.levylab.oxford.Oxford1820","title":"<code>flex_drivers.levylab.oxford.Oxford1820</code>","text":"<p>               Bases: <code>ZMQInstrument</code>, <code>IFMagnetCommands</code></p> <p>Oxford 1820 Magnet subsystem (MNK fridge).</p>"},{"location":"reference/drivers/levylab-oxford_vrm/","title":"<code>levylab.oxford_vrm</code>","text":"<p>LabVIEW class: <code>Instrument.OxfordVRM.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-oxford_vrm/#flex_drivers.levylab.oxford.OxfordVRM","title":"<code>flex_drivers.levylab.oxford.OxfordVRM</code>","text":"<p>               Bases: <code>ZMQInstrument</code>, <code>IFMagnetCommands</code></p> <p>Oxford VRM Magnet subsystem (CF900 fridge).</p> <p>v1 note: \"Not intended for direct use - use MNK class instead.\"</p>"},{"location":"reference/drivers/levylab-ppms/","title":"<code>levylab.ppms</code>","text":"<p>LabVIEW class: <code>instrument.PPMS.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-ppms/#flex_drivers.levylab.ppms.PPMS","title":"<code>flex_drivers.levylab.ppms.PPMS</code>","text":"<p>               Bases: <code>ZMQInstrument</code>, <code>IFTemperatureCommands</code>, <code>IFMagnetCommands</code></p> <p>PPMS driver with Temperature and Magnet capabilities.</p>"},{"location":"reference/drivers/levylab-ppms/#flex_drivers.levylab.ppms.PPMS.get_lhe_level","title":"<code>get_lhe_level()</code>","text":"<p>Read the liquid-helium level (IF <code>getLHeLevel</code>).</p>"},{"location":"reference/drivers/levylab-tc_cf/","title":"<code>levylab.tc_cf</code>","text":"<p>LabVIEW class: <code>Inst.TC.CF.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-tc_cf/#flex_drivers.levylab.tc.TC_CF","title":"<code>flex_drivers.levylab.tc.TC_CF</code>","text":"<p>               Bases: <code>_LeidenTC</code></p> <p>Leiden TC (Z Bridge) on the CF900 fridge.</p>"},{"location":"reference/drivers/levylab-tc_mnk/","title":"<code>levylab.tc_mnk</code>","text":"<p>LabVIEW class: <code>Inst.TC.MNK.lvclass</code> (auto-connected by <code>CESession</code>)</p>"},{"location":"reference/drivers/levylab-tc_mnk/#flex_drivers.levylab.tc.TC_MNK","title":"<code>flex_drivers.levylab.tc.TC_MNK</code>","text":"<p>               Bases: <code>_LeidenTC</code></p> <p>Leiden TC (AVS47B) on the MNK fridge.</p>"},{"location":"reference/drivers/levylab-transport_server/","title":"<code>levylab.transport_server</code>","text":""},{"location":"reference/drivers/levylab-transport_server/#flex_drivers.levylab.transport_server.TransportServer","title":"<code>flex_drivers.levylab.transport_server.TransportServer</code>","text":"<p>               Bases: <code>ZMQInstrument</code></p> <p>Transport measurement orchestration server (LevyLab IF app).</p>"},{"location":"reference/drivers/levylab-transport_server/#flex_drivers.levylab.transport_server.TransportServer.start_transport","title":"<code>start_transport(vi)</code>","text":"<p>Start a transport VI (IF <code>startTransport</code>).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>vi</code> is not one of the allowed VI names.</p>"},{"location":"reference/drivers/levylab-transport_server/#flex_drivers.levylab.transport_server.TransportServer.set_expt_param","title":"<code>set_expt_param(param, value)</code>","text":"<p>Set one experiment parameter (IF <code>setExptParam</code>).</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>value</code> is not a str/int/float or a list thereof.</p>"},{"location":"reference/drivers/levylab-transport_server/#flex_drivers.levylab.transport_server.TransportServer.set_refresh_time","title":"<code>set_refresh_time(refresh_time)</code>","text":"<p>Set the data refresh time in milliseconds (IF <code>setRefreshTime</code>).</p>"},{"location":"reference/drivers/levylab-transport_server/#flex_drivers.levylab.transport_server.TransportServer.lockin_sweep","title":"<code>lockin_sweep(expt_folder, expt_comments, sweep_config, run_continuous=False, timeout=10)</code>","text":"<p>Perform a lock-in sweep with the specified configuration.</p>"},{"location":"reference/drivers/levylab-transport_server/#flex_drivers.levylab.transport_server.TransportServer.get_expt_details","title":"<code>get_expt_details()</code>","text":"<p>Return <code>(experiment folder, experiment comments)</code>.</p>"},{"location":"reference/drivers/rotrics-dexarm/","title":"<code>rotrics.dexarm</code>","text":""},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm","title":"<code>flex_drivers.rotrics.dexarm.DexArm</code>","text":"<p>               Bases: <code>SerialInstrument</code></p> <p>Rotrics DexArm robot arm.</p> <p>Speaks G-code over serial at 115200 baud. Commands embed their own terminators (<code>\\r</code> / <code>\\r\\n</code>) exactly as the firmware expects, and the arm acknowledges each command with a line containing <code>ok</code> \u2014 so this driver talks to the serial port directly through :meth:<code>_send_cmd</code> rather than the line-oriented base <code>write</code>/<code>read</code>.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.go_home","title":"<code>go_home()</code>","text":"<p>Go to the home position and enable the motors.</p> <p>Should be called each time the arm is powered on.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.set_workorigin","title":"<code>set_workorigin()</code>","text":"<p>Set the current position as the new work origin.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.set_acceleration","title":"<code>set_acceleration(acceleration, travel_acceleration, retract_acceleration=60)</code>","text":"<p>Set the preferred starting acceleration for moves of different types.</p> <p>Parameters:</p> Name Type Description Default <code>acceleration</code> <code>int</code> <p>Printing acceleration. Used for moves that employ the current tool.</p> required <code>travel_acceleration</code> <code>int</code> <p>Used for moves that include no extrusion.</p> required <code>retract_acceleration</code> <code>int</code> <p>Used for extruder retraction moves.</p> <code>60</code>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.set_module_type","title":"<code>set_module_type(module_type)</code>","text":"<p>Set the type of end effector.</p> <p>Parameters:</p> Name Type Description Default <code>module_type</code> <code>int</code> <p>0 for pen holder module, 1 for laser engraving module, 2 for pneumatic module, 3 for 3D printing module.</p> required"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.get_module_type","title":"<code>get_module_type()</code>","text":"<p>Get the type of end effector.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>'PEN', 'LASER', 'PUMP', or '3D' (None if the arm did not report</p> <code>str | None</code> <p>a recognized module before acknowledging).</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.move_to","title":"<code>move_to(x=None, y=None, z=None, e=None, feedrate=2000, mode='G1', wait=True)</code>","text":"<p>Move to a cartesian position.</p> <p>Adds a linear move to the queue, performed after all previous moves complete.</p> <p>Position is in millimeters by default (inches after G20); the center of the y axis is 300 mm.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p><code>\"G1\"</code> (default) or <code>\"G0\"</code> for fast mode.</p> <code>'G1'</code> <code>feedrate</code> <code>int</code> <p>Sets the feedrate for all subsequent moves.</p> <code>2000</code> <code>wait</code> <code>bool</code> <p>Block until the arm acknowledges the move.</p> <code>True</code>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.fast_move_to","title":"<code>fast_move_to(x=None, y=None, z=None, feedrate=2000, wait=True)</code>","text":"<p>Fast move to a cartesian position (mode G0), in millimeters.</p> <p>Parameters:</p> Name Type Description Default <code>feedrate</code> <code>int</code> <p>Sets the feedrate for all subsequent moves.</p> <code>2000</code> <code>wait</code> <code>bool</code> <p>Block until the arm acknowledges the move.</p> <code>True</code>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.get_current_position","title":"<code>get_current_position()</code>","text":"<p>Get the current position.</p> <p>Returns:</p> Type Description <code>float | None</code> <p>Position <code>(x, y, z)</code>, extrusion <code>e</code>, and DexArm theta</p> <code>float | None</code> <p><code>(a, b, c)</code>.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.delay_ms","title":"<code>delay_ms(value)</code>","text":"<p>Pause the command queue for <code>value</code> milliseconds.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.delay_s","title":"<code>delay_s(value)</code>","text":"<p>Pause the command queue for <code>value</code> seconds.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.soft_gripper_pick","title":"<code>soft_gripper_pick()</code>","text":"<p>Close the soft gripper.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.soft_gripper_place","title":"<code>soft_gripper_place()</code>","text":"<p>Wide-open the soft gripper.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.soft_gripper_nature","title":"<code>soft_gripper_nature()</code>","text":"<p>Release the soft gripper to its natural state.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.soft_gripper_stop","title":"<code>soft_gripper_stop()</code>","text":"<p>Stop the soft gripper.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.air_picker_pick","title":"<code>air_picker_pick()</code>","text":"<p>Pick up an object.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.air_picker_place","title":"<code>air_picker_place()</code>","text":"<p>Release an object.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.air_picker_nature","title":"<code>air_picker_nature()</code>","text":"<p>Release to natural state.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.air_picker_stop","title":"<code>air_picker_stop()</code>","text":"<p>Stop the picker.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.laser_on","title":"<code>laser_on(value=0)</code>","text":"<p>Turn on the laser.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>int</code> <p>Set the power, range from 1 to 255.</p> <code>0</code>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.laser_off","title":"<code>laser_off()</code>","text":"<p>Turn off the laser.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.conveyor_belt_forward","title":"<code>conveyor_belt_forward(speed=0)</code>","text":"<p>Move the belt forward.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.conveyor_belt_backward","title":"<code>conveyor_belt_backward(speed=0)</code>","text":"<p>Move the belt backward.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.conveyor_belt_stop","title":"<code>conveyor_belt_stop()</code>","text":"<p>Stop the belt.</p>"},{"location":"reference/drivers/rotrics-dexarm/#flex_drivers.rotrics.dexarm.DexArm.sliding_rail_init","title":"<code>sliding_rail_init()</code>","text":"<p>Initialize the sliding rail.</p>"},{"location":"reference/drivers/srs-sr7270/","title":"<code>srs.sr7270</code>","text":""},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270","title":"<code>flex_drivers.srs.sr7270.SR7270</code>","text":"<p>               Bases: <code>VISAInstrument</code></p> <p>Driver for the 7270 DSP lock-in amplifier via its RAW USB VISA resource.</p> <p>The instrument frames every USB response as <code>&lt;text&gt;\\x00&lt;status&gt;&lt;overload&gt;</code>: the payload is NULL-terminated and followed by a status byte and an overload byte. Every command \u2014 including sets \u2014 produces such a reply, so all traffic goes through :meth:<code>query</code>, which returns the <code>(text, status, overload)</code> tuple.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.query","title":"<code>query(cmd)</code>","text":"<p>Send a command and read the framed response.</p> <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[str, int, int]</code> <p><code>(response_str, status_byte, overload_byte)</code></p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.read","title":"<code>read()</code>","text":"<p>Read chunks until the NULL + status + overload frame is complete.</p> <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[str, int, int]</code> <p><code>(response_str, status_byte, overload_byte)</code></p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_id","title":"<code>get_id()</code>","text":"<p>Get instrument identification string.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_ver","title":"<code>get_ver()</code>","text":"<p>Get instrument firmware version.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.set_ref_frequency","title":"<code>set_ref_frequency(freq_hz)</code>","text":"<p>Set internal oscillator frequency in Hz.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_ref_frequency","title":"<code>get_ref_frequency()</code>","text":"<p>Get internal oscillator frequency in Hz.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.set_ref_amplitude","title":"<code>set_ref_amplitude(amp_volts)</code>","text":"<p>Set oscillator amplitude in Volts.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_ref_amplitude","title":"<code>get_ref_amplitude()</code>","text":"<p>Get oscillator amplitude in Volts.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.set_ref_phase","title":"<code>set_ref_phase(phase_deg)</code>","text":"<p>Set reference phase shift in degrees.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_ref_phase","title":"<code>get_ref_phase()</code>","text":"<p>Get reference phase shift in degrees.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.set_sensitivity","title":"<code>set_sensitivity(index)</code>","text":"<p>Set full-scale sensitivity range (1-27, see manual).</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_sensitivity","title":"<code>get_sensitivity()</code>","text":"<p>Get current sensitivity range index.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.set_time_constant","title":"<code>set_time_constant(index)</code>","text":"<p>Set output time constant (0-29, see manual).</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_time_constant","title":"<code>get_time_constant()</code>","text":"<p>Get current time constant index.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.auto_measure","title":"<code>auto_measure()</code>","text":"<p>Perform Auto-Measure (ASM) operation.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_x","title":"<code>get_x()</code>","text":"<p>Get X channel output in Volts.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_y","title":"<code>get_y()</code>","text":"<p>Get Y channel output in Volts.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_magnitude","title":"<code>get_magnitude()</code>","text":"<p>Get magnitude output in Volts.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_phase","title":"<code>get_phase()</code>","text":"<p>Get phase output in degrees.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_xy","title":"<code>get_xy()</code>","text":"<p>Get (X, Y) tuple in Volts.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_rtheta","title":"<code>get_rtheta()</code>","text":"<p>Get (Magnitude, Phase) tuple.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.setup_curve_buffer","title":"<code>setup_curve_buffer(buffer_size)</code>","text":"<p>Set curve buffer length.</p> <p>Parameters:</p> Name Type Description Default <code>buffer_size</code> <code>int</code> <p>Number of points to store (max 100000).</p> required"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.configure_curve_quantity","title":"<code>configure_curve_quantity(cbd_value)</code>","text":"<p>Configure what quantities to store in the curve buffer.</p> <p>NOTE: For floating point readout (<code>DC.</code> command), bit 4 (Sensitivity=16) MUST be included when storing X, Y, Magnitude, or Noise.</p> <p>Parameters:</p> Name Type Description Default <code>cbd_value</code> <code>int</code> <p>Bit mask - combine with bitwise OR: 1=X, 2=Y, 4=Mag, 8=Phase, 16=Sensitivity, 32=ADC1, 64=ADC2, 128=ADC3</p> required <p>Examples:</p> <p>17 (1+16): X + Sensitivity (floating point) 19 (1+2+16): X + Y + Sensitivity (floating point)</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.set_storage_interval","title":"<code>set_storage_interval(interval_us)</code>","text":"<p>Set time interval between data points.</p> <p>Parameters:</p> Name Type Description Default <code>interval_us</code> <code>float</code> <p>Interval in microseconds (min 1000 us = 1 ms).</p> required"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.start_continuous_acquisition","title":"<code>start_continuous_acquisition(mode=0)</code>","text":"<p>Start continuous data acquisition.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>int</code> <p>0=start immediately, 1=start on trigger, 2=triggered start/stop.</p> <code>0</code>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.halt_acquisition","title":"<code>halt_acquisition()</code>","text":"<p>Stop curve acquisition.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_acquisition_status","title":"<code>get_acquisition_status()</code>","text":"<p>Get curve acquisition status.</p> <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[int, ...]</code> <p><code>(status, num_sweeps, status_byte, num_points)</code> status: 0=idle, 2=running, 6=halted num_points: Number of points acquired</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.get_num_stored_points","title":"<code>get_num_stored_points()</code>","text":"<p>Get number of points stored in buffer.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.read_curve_buffer","title":"<code>read_curve_buffer(bit_number)</code>","text":"<p>Read data from the curve buffer.</p> <p>Parameters:</p> Name Type Description Default <code>bit_number</code> <code>int</code> <p>Bit position from CBD (0=X, 1=Y, 2=Mag, 3=Phase, etc.)</p> required <p>Returns:</p> Type Description <code>list[float]</code> <p>list of float values</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.clear_curve_buffer","title":"<code>clear_curve_buffer()</code>","text":"<p>Clear the curve buffer.</p>"},{"location":"reference/drivers/srs-sr7270/#flex_drivers.srs.sr7270.SR7270.acquire_continuous","title":"<code>acquire_continuous(duration_s, sample_rate_hz, channel='X')</code>","text":"<p>Perform continuous data acquisition and return time-series data.</p> <p>Parameters:</p> Name Type Description Default <code>duration_s</code> <code>float</code> <p>Acquisition duration in seconds.</p> required <code>sample_rate_hz</code> <code>float</code> <p>Sample rate in Hz (max 1000 Hz).</p> required <code>channel</code> <code>str</code> <p>Channel to acquire ('X', 'Y', 'Mag', or 'Phase').</p> <code>'X'</code> <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[ndarray, ndarray]</code> <p><code>(timestamps, values)</code> as numpy arrays. timestamps: Time array in seconds. values: Measured values in Volts (or degrees for Phase).</p> Example <p>times, x_vals = sr.acquire_continuous(duration_s=10.0, sample_rate_hz=100)</p>"},{"location":"reference/integrations/","title":"Integrations","text":"<p>Generated from the package catalog at build time.</p> Package Provides Summary <code>flex-asana</code> <code>comms: asana</code> Asana comms backend: an experiment task per run, via the Asana API directly <code>flex-datatypes</code> <code>writer: hdf5, tdms</code> HDF5 and TDMS (LabVIEW-compatible) data file writers <code>flex-nextcloud</code> <code>storage: nextcloud</code> Nextcloud storage backend (WebDAV upload)"},{"location":"reference/integrations/SUMMARY/","title":"SUMMARY","text":"<ul> <li>Overview</li> <li>flex-asana</li> <li>flex-datatypes</li> <li>flex-nextcloud</li> </ul>"},{"location":"reference/integrations/flex-asana/","title":"<code>flex-asana</code>","text":"<p>Asana comms backend: an experiment task per run, via the Asana API directly</p> <pre><code>flex install flex-asana\n</code></pre>"},{"location":"reference/integrations/flex-asana/#asana","title":"<code>asana</code>","text":""},{"location":"reference/integrations/flex-asana/#flex_asana.comms.AsanaComms","title":"<code>flex_asana.comms.AsanaComms</code>","text":"<p>               Bases: <code>CommsBackend</code></p> <p>Creates an Asana task when an experiment starts (stamped with the start time, assigned to the user) and fills in the end time when it ends. See :class:<code>~flex_asana.sync.ExperimentSync</code> for the env vars this reads (<code>ASANA_ACCESS_TOKEN</code>, <code>ASANA_WORKSPACE_GID</code>, <code>ASANA_EXPERIMENTS_PROJECT_GID</code>, <code>ASANA_START_FIELD</code>, <code>ASANA_END_FIELD</code>). Any argument here overrides the matching env var.</p>"},{"location":"reference/integrations/flex-datatypes/","title":"<code>flex-datatypes</code>","text":"<p>HDF5 and TDMS (LabVIEW-compatible) data file writers</p>"},{"location":"reference/integrations/flex-datatypes/#hdf5","title":"<code>hdf5</code>","text":""},{"location":"reference/integrations/flex-datatypes/#flex_datatypes.hdf5.HDF5Writer","title":"<code>flex_datatypes.hdf5.HDF5Writer</code>","text":"<p>               Bases: <code>DataWriter</code></p>"},{"location":"reference/integrations/flex-datatypes/#tdms","title":"<code>tdms</code>","text":""},{"location":"reference/integrations/flex-datatypes/#flex_datatypes.tdms.TDMSWriter","title":"<code>flex_datatypes.tdms.TDMSWriter</code>","text":"<p>               Bases: <code>DataWriter</code></p>"},{"location":"reference/integrations/flex-nextcloud/","title":"<code>flex-nextcloud</code>","text":"<p>Nextcloud storage backend (WebDAV upload)</p> <pre><code>flex install flex-nextcloud\n</code></pre>"},{"location":"reference/integrations/flex-nextcloud/#nextcloud","title":"<code>nextcloud</code>","text":""},{"location":"reference/integrations/flex-nextcloud/#flex_nextcloud.storage.NextcloudStorage","title":"<code>flex_nextcloud.storage.NextcloudStorage</code>","text":"<p>               Bases: <code>StorageBackend</code></p>"},{"location":"reference/integrations/flex-nextcloud/#flex_nextcloud.storage.NextcloudStorage.open_local","title":"<code>open_local(pointer)</code>","text":"<p>Prefer the local cache; download from Nextcloud if it is gone.</p>"},{"location":"tutorials/write-a-driver/","title":"Write a driver","text":"<p>A FLEX driver is one class: a protocol base supplies the connection, you declare parameters and methods. This tutorial writes a driver for a fictitious <code>Acme4000</code> source-meter and ends with the optional steps \u2014 registration, LevyLab integration, hardware-free tests.</p> <p>Background on the pieces: Instruments &amp; drivers.</p>"},{"location":"tutorials/write-a-driver/#1-pick-the-protocol-base","title":"1. Pick the protocol base","text":"<p>Match how the instrument is physically connected:</p> Connection Base Constructor takes GPIB, USB-TMC, VISA of any kind <code>VISAInstrument</code> <code>resource</code> string Raw TCP socket, line-based text <code>TCPInstrument</code> <code>host</code>, <code>port</code> COM port <code>SerialInstrument</code> <code>port</code> (plus <code>baudrate=</code>) JSON-RPC over ZMQ (LevyLab IF apps) <code>ZMQInstrument</code> <code>address</code> <p>All connect eagerly in <code>__init__</code> \u2014 constructing the driver either connects or raises.</p>"},{"location":"tutorials/write-a-driver/#2-scaffold-the-file","title":"2. Scaffold the file","text":"<pre><code>flex new driver Acme4000 --protocol visa\n</code></pre> <p>This writes <code>acme4000.py</code> in the current directory (change with <code>--out &lt;dir&gt;</code>; it refuses to overwrite an existing file). Protocols: <code>visa</code> (default) | <code>tcp</code> | <code>serial</code> | <code>zmq</code>. The generated file:</p> <pre><code>\"\"\"FLEX driver for Acme4000.\"\"\"\n\nfrom flex_protocols import VISAInstrument\n\n\nclass Acme4000(VISAInstrument):\n    def __init__(self, name: str = \"acme4000\", resource: str = \"GPIB0::1::INSTR\", **kwargs):\n        super().__init__(name, resource, **kwargs)\n\n        # Declare parameters: name, commands (or getter/setter), unit.\n        ...\n</code></pre>"},{"location":"tutorials/write-a-driver/#3-fill-in-parameters-and-methods","title":"3. Fill in parameters and methods","text":"<pre><code>from flex.instrument import Numbers\nfrom flex_protocols import VISAInstrument\n\n\nclass Acme4000(VISAInstrument):\n    def __init__(self, name: str = \"acme4000\", resource: str = \"GPIB0::1::INSTR\", **kwargs):\n        super().__init__(name, resource, **kwargs)\n\n        self.voltage = self.add_parameter(\n            \"voltage\", get_cmd=\"SOUR:VOLT?\", set_cmd=\"SOUR:VOLT {}\",\n            get_parser=float, unit=\"V\", vals=Numbers(-20, 20),\n        )\n        self.current = self.add_parameter(\n            \"current\", get_cmd=\"MEAS:CURR?\", get_parser=float, unit=\"A\",\n        )\n\n    def output(self, on: bool) -&gt; None:\n        self.write(f\"OUTP {'ON' if on else 'OFF'}\")\n</code></pre> <p>Guidelines:</p> <ul> <li>Anything you will sweep or measure should be a parameter \u2014 sweeps and data   files pick up its name and unit automatically.</li> <li>One-shot actions (reset, output on/off, autorange) are plain methods.</li> <li>Use <code>vals=</code> to reject dangerous setpoints before they reach the hardware.</li> <li>The base class already gives you <code>idn()</code>, <code>snapshot()</code>, <code>close()</code>, logging,   and context-manager support; override <code>idn()</code> only for non-SCPI   instruments.</li> </ul>"},{"location":"tutorials/write-a-driver/#4-use-it-no-registration-needed","title":"4. Use it \u2014 no registration needed","text":"<p>A driver is just a class. Import it and go:</p> <pre><code>from acme4000 import Acme4000\nfrom flex import Experiment, Scan, sweep\nimport numpy as np\n\nwith Experiment(\"jane\") as exp:\n    src = exp.add(Acme4000, resource=\"GPIB0::24::INSTR\")\n    Scan(sweep(src.voltage, np.linspace(0, 1, 51), delay=0.1)) \\\n        .measure(src.current) \\\n        .on_abort(lambda: src.voltage(0)) \\\n        .run(exp, name=\"IV\")\n</code></pre> <p>For many drivers this is the end of the tutorial.</p>"},{"location":"tutorials/write-a-driver/#5-optional-register-it-for-name-based-resolution","title":"5. Optional: register it for name-based resolution","text":"<p>Registration only matters if you want the driver addressable by name \u2014 in <code>[stations.*]</code> config blocks, <code>flex enable</code>, <code>flex instruments --probe</code>, and the dashboard.</p> <p>Your own package. Scaffold one and move the driver file in:</p> <pre><code>flex new package flex-drivers-mylab\n</code></pre> <p>This creates an installable package (<code>pyproject.toml</code>, <code>src/flex_drivers_mylab/</code>, <code>tests/</code>) whose <code>__init__.py</code> holds the registry dict:</p> <pre><code>CATALOG: dict[str, str] = {\n    \"mylab.acme4000\": \"flex_drivers_mylab.acme4000:Acme4000\",\n}\n</code></pre> <p>Install it (<code>pip install -e flex-drivers-mylab</code>), then point FLEX at the registry with a <code>catalog.local.json</code> next to your active ecosystem config:</p> <pre><code>{\"flex-drivers-mylab\": {\"registries\": {\"drivers\": \"flex_drivers_mylab:CATALOG\"}}}\n</code></pre> <p>The local catalog is merged over the built-in one at load time, so your drivers now appear in <code>flex list --drivers</code> and resolve by name everywhere.</p> <p>Or contribute to <code>flex-drivers</code>. Add a vendor folder (<code>src/flex_drivers/acme/</code>), put the module there, and add one line to the package's <code>CATALOG</code> in <code>src/flex_drivers/__init__.py</code>. The existing vendor folders (<code>srs/</code>, <code>colby/</code>, <code>rotrics/</code>) are the pattern to copy.</p>"},{"location":"tutorials/write-a-driver/#6-levylab-instrument-framework-drivers","title":"6. LevyLab Instrument-Framework drivers","text":"<p>An IF app driver inherits <code>ZMQInstrument</code> and sets <code>lv_class</code> to the app's LabVIEW class name so <code>CESession</code> can auto-connect it:</p> <pre><code>from flex_drivers.levylab._commands import IFTemperatureCommands\nfrom flex_protocols import ZMQInstrument\n\n\nclass MyCryostat(ZMQInstrument, IFTemperatureCommands):\n    lv_class = \"Instrument.MyCryostat.lvclass\"\n\n    def get_pressure(self) -&gt; float:\n        return self.call(\"getPressure\")\n</code></pre> <ul> <li>Wire methods are <code>self.call(\"&lt;IF method name&gt;\", params)</code>; keep the exact   IF spellings on the wire, snake_case on the Python side.</li> <li>If the app implements the standard IF temperature or magnet commands, mix   in <code>IFTemperatureCommands</code> / <code>IFMagnetCommands</code> from   <code>flex_drivers.levylab._commands</code> instead of rewriting them.</li> <li>Add the driver to the levylab <code>CATALOG</code>; <code>lvclass_registry()</code> picks up   <code>lv_class</code> (and <code>lv_class_aliases</code>) automatically \u2014 there is no separate   mapping to maintain.</li> </ul>"},{"location":"tutorials/write-a-driver/#7-test-without-hardware","title":"7. Test without hardware","text":"<p>For ZMQ drivers, <code>flex_protocols.testing.FakeIFServer</code> is an in-process fake IF app: give it canned results per method, then assert on both the returned values and the exact wire traffic it recorded. This is how every LevyLab driver in the repo is tested (<code>packages/flex-drivers/tests/test_levylab_drivers.py</code>):</p> <pre><code>from flex_protocols.testing import FakeIFServer\n\n\ndef test_pressure():\n    with FakeIFServer({\"getPressure\": 1.3e-6}) as server:\n        with MyCryostat(\"cryo\", server.address) as dev:\n            assert dev.get_pressure() == 1.3e-6\n        assert server.requests[-1][\"method\"] == \"getPressure\"\n</code></pre> <p>Handlers may be callables (<code>params -&gt; result</code>) and may raise to produce a JSON-RPC error; set <code>server.delay</code> to test timeout recovery.</p> <p>For VISA/TCP/serial drivers there is no bundled simulator \u2014 the repo's pattern (see <code>packages/flex-drivers/tests/test_sr7270.py</code>) is a small fake resource class plus a monkeypatched <code>ResourceManager</code>, asserting on the commands the driver writes. <code>SimulatedInstrument</code> covers the cases where you only need canned query/reply behavior on top of the base <code>Instrument</code>.</p>"},{"location":"tutorials/write-a-driver/#checklist","title":"Checklist","text":"<ul> <li>[ ] Correct protocol base; connection parameters have sensible defaults</li> <li>[ ] Sweepable/measurable quantities are parameters, with units</li> <li>[ ] Validators on anything that could damage a sample</li> <li>[ ] Driver works by direct import</li> <li>[ ] (optional) <code>CATALOG</code> entry + <code>catalog.local.json</code>, or a <code>flex-drivers</code> PR</li> <li>[ ] (LevyLab) <code>lv_class</code> set; command mixins used where they apply</li> <li>[ ] Tests pass without hardware</li> </ul>"}]}